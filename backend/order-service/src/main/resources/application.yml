server:
  port: 8004

spring:
  application:
    name: order-service
  profiles:
    active: local
  config:
    import: "configserver:"

logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.ashina.ecommerce: DEBUG
